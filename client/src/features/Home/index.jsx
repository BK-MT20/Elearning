import React from "react";
import PropTypes from "prop-types";

Home.propTypes = {};

function Home(props) {
  return (
    <div className="home-page" style={{ minHeight: 'calc(100vh - 88px)', background: 'center bottom / cover no-repeat url("./background.png")' }}>
    </div>
  );
}

export default Home;
